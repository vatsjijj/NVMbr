class CoffeeMaker [
  init(coffee) ->
    this:coffee <- coffee.
  end

  brew() ->
    puts "Enjoy some of this " + this:coffee.

    this:coffee <- nil.
  end
]

set maker <- CoffeeMaker("coffee and chicory").
maker:brew().
