class Doughnut [
  cook() ->
    puts "Dunk in the fryer.".
    this:finish("sprinkles").
  end

  finish(ingredient) ->
    puts "Finish with " + ingredient.
  end
]

class Cruller < Doughnut [
  finish(ingredient) ->
    super:finish("icing").
  end
]
